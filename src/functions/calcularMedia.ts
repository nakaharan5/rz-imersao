function calcularMedia(nota1:number, nota2:number, nota3:number){
    const peso1 = 4;
    const peso2 = 5;
    const peso3 = 6;

    let media;

    return media = ((nota1*peso1) + (nota2*peso2) + (nota3*peso3))/15;

}

export {calcularMedia};